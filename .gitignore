# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment Variables
.env
.env.local
.env.*.local
!.env.example

# Test outputs
coverage/
test-results/
playwright-report/
.playwright/
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Server ────────────────────────────────────────────────────────────────────
server-nest/.env
server-nest/.env.local
server-nest/dist
server-nest/node_modules
server-nest/coverage

# ── K8s — never commit real secret values ─────────────────────────────────────
# The secret.yaml template IS committed (it has placeholder values).
# If you create a filled version with real keys, name it secret.local.yaml
# and it will be ignored by this rule.
k8s/*.local.yaml

# ── Misc ──────────────────────────────────────────────────────────────────────
*.pem
*.key
*.cert
